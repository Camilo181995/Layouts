<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:Layouts" x:Class="Layouts.MainPage">

    <StackLayout Orientation="Horizontal">

        <Button BackgroundColor="Blue"
                TextColor="Silver"
               Text="Fill"
               VerticalOptions="Fill"/>

         <Button BackgroundColor="Blue"
                TextColor="Silver"
               Text="Start"
               VerticalOptions="Start"/>

        <Button BackgroundColor="Blue"
                TextColor="Silver"
               Text="Center"
               VerticalOptions="Center"/>

        <Button BackgroundColor="Blue"
                TextColor="Silver"
               Text="End"
               VerticalOptions="End"/>
    </StackLayout>
    
</ContentPage>